import React from 'react';

// import css from './buttons.module.scss'

const PrevBtn = () => {
  return (
    <button className={`${css.btnPrev} ${css.buttons} previous`} type="button">
      PrevBtn
    </button>
  );
};

export default PrevBtn;
